# Development / CI dependencies (not included in the Docker image)
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
respx>=0.21.0        # Mock httpx requests in tests
black>=24.0.0
ruff>=0.3.0
mypy>=1.9.0
